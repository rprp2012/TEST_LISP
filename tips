1.类,变量,函数的命名空间互相独立.

2. #x十六进制.
   #b二进制
   #\A 字符A
   #P"..."路径名
   #' function

3. let与let*的区别:
(let* ((x 5) (y x)) ...)
let不支持在后续变量的初始化中使用之前变量.

let的所有初始化同时进行,效率更高.

4. load时,
	在宏展开期将所有宏展开
	在编译器编译实际代码

5. 引用和反引用
	最外层的反引号对应最内层的逗号,且层数必须相等.
	即外3的反引号只会消耗内3的逗号.
    同时,编译器自动优化会消去",'".

	逗号搭配list函数会出现奇怪的情况,不要一起使用,不要出现",(list 1 2)".
